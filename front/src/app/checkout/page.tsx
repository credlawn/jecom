import CheckoutPage from '@/components/checkout/CheckoutPage';

// This component is the route entry point for /checkout
// It simply renders the main CheckoutPage component which contains all the logic.

export default function CheckoutRoute() {
  return <CheckoutPage />;
}
